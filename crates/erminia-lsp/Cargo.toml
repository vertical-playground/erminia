[package]
name = "erminia-lsp"
version = "0.1.0"
edition = "2024"
license = "MIT"

[[bin]]
name = "erminia-lsp"
path = "src/main.rs"

[dependencies]
erminia = {path = "../erminia"}
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
