[package]
name = "erminia-cli"
version = "0.1.0"
edition = "2024"
license = "MIT"

[[bin]]
name = "erminia"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
erminia = {path = "../erminia"}
rustyline = "15"
